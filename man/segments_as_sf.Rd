% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{segments_as_sf}
\alias{segments_as_sf}
\title{Convert segments to spatial features}
\usage{
segments_as_sf(x, what = c("midpoints", "lines"), crs = 4326)
}
\arguments{
\item{x}{A \code{distsamp_segments} object.}

\item{what}{\code{"midpoints"} (default) for one point per segment, or \code{"lines"}
for the trackline each segment covers.}

\item{crs}{Coordinate reference system for the result. Default \code{4326}
(WGS 84), the datum NARWC coordinates are recorded in.}
}
\value{
An \code{sf} object with one feature per segment, carrying the segment
attributes.
}
\description{
Builds an \code{sf} object from a \code{\link[=segment_survey]{segment_survey()}} result, either as the segment
midpoints or as the trackline geometry of each segment.
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
  segs <- segment_survey(read_narwc(path), seg_length = 5, seed = 1)
  segments_as_sf(segs)
}

}
\references{
Pebesma, E. (2018) Simple features for R: standardized support for spatial
vector data. \emph{The R Journal} 10(1):439-446. \doi{10.32614/RJ-2018-009}
}
