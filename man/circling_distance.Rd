% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exact.R
\name{circling_distance}
\alias{circling_distance}
\title{Perpendicular distances for sightings made while circling}
\usage{
circling_distance(
  dat,
  by = NULL,
  units = c("m", "km", "nmi"),
  position = c("exact", "logged")
)
}
\arguments{
\item{dat}{A data frame of NARWC survey data in survey order, with \code{LATITUDE},
\code{LONGITUDE}, \code{LEGTYPE}, and ideally \code{CIRCLE} from \code{\link[=flag_circling]{flag_circling()}} and
\code{S_LAT}/\code{S_LONG}. Without \code{CIRCLE}, \code{LEGTYPE == 4} is used.}

\item{by}{Grouping columns identifying one occupation of a survey line, as in
\code{\link[=track_bearing]{track_bearing()}}.}

\item{units}{\code{"m"} (default), \code{"km"}, or \code{"nmi"}.}

\item{position}{Which position to treat as the animal's. \code{"exact"} (default)
uses \code{S_LAT}/\code{S_LONG} only. \code{"logged"} falls back to the record's own
\code{LATITUDE}/\code{LONGITUDE} where no exact position exists — but that is the
\emph{aircraft} orbiting the animal, offset by the radius of the circle, which at
a few hundred metres is the same size as the distances being measured. Use
it knowing that, and read \code{position_source} to see which applied.}
}
\value{
A tibble with one row per row of \code{dat}, populated only for circling
sightings:
\describe{
\item{\code{distance}}{Perpendicular distance from the census line.}
\item{\code{along}}{Signed along-track distance from the break-off point to
where the animal was abeam; negative means back down the line.}
\item{\code{side}}{\code{"left"}, \code{"right"}, \code{"on-track"}, or \code{NA}.}
\item{\code{radial}}{Straight-line distance from the break-off point.}
\item{\code{bearing}}{Inbound track bearing at the anchor.}
\item{\code{anchor_event}}{\code{EVENTNO} of the anchor record, or its row number
when \code{EVENTNO} is absent.}
\item{\code{position_source}}{\code{"exact"} or \code{"logged"}.}
}
}
\description{
A group spotted from the census track is often circled for photographs,
identification, and a proper count, and the sighting is logged during that
circle rather than at the moment of detection. This ties each such sighting
back to the point on the track-line where the aircraft broke off, and measures
the distance from there.
}
\section{Why these detections matter}{

A circling sighting is usually a genuine on-effort detection: the animal was
seen from the track-line, which is why the aircraft left it. Giving it no
distance drops it from the detection function altogether, and the dropped
detections are not a random sample — they are disproportionately the close,
conspicuous, or high-priority groups that were worth breaking off for. Losing
those thins the near-zero end of the distance distribution, exactly where a
detection function is most sensitive.
}

\section{The anchor}{

The reference point is the last census record before the circling began — the
\code{LEGSTAGE == 3} break-off record where one exists (handbook 8.A.20), and
otherwise the last record still on the line. \code{anchor_event} reports which
record was used so the choice can be checked.

The bearing is the \emph{inbound} heading: from the previous distinct census
position to the anchor, which is the direction the aircraft was flying when
the animal was detected. It is deliberately not the centred difference
\code{\link[=track_bearing]{track_bearing()}} uses, because the record after a break-off is the resume
record, and a resume point offset from the break-off would swing the bearing
by an amount that has nothing to do with the track being flown.
}

\section{Break-off point, or the line through it}{

The aircraft usually flies past a group before turning, so the break-off point
is beyond the animal rather than abeam of it. \code{radial} is the straight-line
distance from the break-off point to the animal; \code{distance} is that position
projected perpendicularly onto the census line, and \code{along} is how far back
along the line the animal was abeam. \strong{\code{distance} is the one a detection
function needs} — \code{radial} will generally be larger, by exactly the margin
\code{along} records.
}

\section{What this cannot fix}{

The position is recorded some minutes after detection, so the animal has
moved, and the distance is an estimate of where it was when detected rather
than a measurement of it. That error is not quantified here. Treat circling
distances as a distinct and less reliable source — which is why
\code{distance_source} marks them, and why they should be excluded from a detection
function unless their inclusion is a deliberate, stated choice.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
dat <- flag_circling(make_leg_id(read_narwc(path)))
d <- circling_distance(dat)
d[!is.na(d$distance), ]

}
\references{
Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}, sections 4.2 (event 12), 8.A.20,
and 8.A.21. NARWC Reference Document 2023-01.
}
\seealso{
\code{\link[=flag_circling]{flag_circling()}}, \code{\link[=attach_circling_sightings]{attach_circling_sightings()}},
\code{\link[=cross_track_distance]{cross_track_distance()}}, \code{\link[=exact_distance]{exact_distance()}}
}
