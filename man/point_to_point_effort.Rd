% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geodist.R
\name{point_to_point_effort}
\alias{point_to_point_effort}
\title{Accumulate point-to-point survey effort}
\usage{
point_to_point_effort(
  dat,
  by = c("FILEID", "LEGNO3"),
  method = c("haversine", "becker", "kenney", "eab", "rdk")
)
}
\arguments{
\item{dat}{A data frame with \code{LATITUDE}, \code{LONGITUDE}, \code{OnOff.Effort}, and the
grouping columns named in \code{by}. Records must already be in survey order;
\code{\link[=read_narwc]{read_narwc()}} does not sort, so sort by \code{DATE}/\code{FILEID}/\code{EVENTNO}
beforehand if you are unsure.}

\item{by}{Character vector of columns identifying a continuous survey line.
Defaults to \code{c("FILEID", "LEGNO3")}, the line-occupation identifier
produced by \code{\link[=make_leg_id]{make_leg_id()}}.}

\item{method}{Distance method passed to \code{\link[=gc_distance]{gc_distance()}}: \code{"haversine"}
(default), \code{"becker"}, or \code{"kenney"}.}
}
\value{
\code{dat} with two columns added or replaced:
\describe{
\item{\code{pt2pt.effort}}{Distance in km from this record to the next
on-effort record within the same line; \code{0} at line ends and across
off-effort gaps.}
\item{\code{Effort}}{Total on-effort distance of the line this record belongs
to, repeated on every record of the line.}
}
}
\description{
Computes the along-track distance from each record to the next within a
survey line, counting only the intervals where the aircraft was on effort at
both ends.
}
\details{
Effort is attributed to the \emph{first} record of each pair, so the last record
of a line has \code{pt2pt.effort} of \code{0}. Intervals with an off-effort endpoint
are \code{0} rather than \code{NA}, so that segment effort sums are well defined
without needing \code{na.rm}.
}
\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
dat <- flag_effort(make_leg_id(read_narwc(path)))
dat <- point_to_point_effort(dat)
sum(dat$pt2pt.effort)

}
\references{
Kenney, R.D. and Winn, H.E. (1986) Cetacean high-use habitats of the
northeast United States continental shelf. \emph{Fishery Bulletin} 84(2):345-357.
Effort is accumulated here as they describe it: "for any pair of successive
positions, the length of track line between the points" summed over the
qualifying records.
}
\seealso{
\code{\link[=gc_distance]{gc_distance()}}, \code{\link[=make_leg_id]{make_leg_id()}}
}
