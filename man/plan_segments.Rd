% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segments.R
\name{plan_segments}
\alias{plan_segments}
\title{Plan how many segments each track carries}
\usage{
plan_segments(tracks, seg_length, seg_tol_frac = 0.5, seed = NULL)
}
\arguments{
\item{tracks}{A tibble from \code{\link[=track_effort]{track_effort()}}.}

\item{seg_length}{Target segment length in km.}

\item{seg_tol_frac}{Tolerance as a fraction of \code{seg_length}, controlling
whether a remainder becomes its own segment or is absorbed. Default \code{0.5},
so segments run between \code{0.5 * seg_length} and \code{1.5 * seg_length}.}

\item{seed}{Integer RNG seed, or \code{NULL} for unseeded.}
}
\value{
A tibble with one row per planned segment: \code{DATE}, \code{new_trackno},
\code{start_time}, \code{track_effort}, \code{seg_no} (position within the track), and
\code{tgtdist} (target length in km).
}
\description{
Decides, for every continuous track, how many segments to cut and what target
length each should have. This is the planning half of the segmentation; the
cutting half is \code{\link[=cut_segments]{cut_segments()}}.
}
\section{The method}{

Adapted from Elizabeth Becker's \code{segchopr} code. For a track of length \code{L}
and a target segment length \code{s}:
\itemize{
\item \code{L} shorter than \code{s} gives a single segment of length \code{L}.
\item Otherwise \code{floor(L / s)} whole segments fit, leaving a remainder
\code{rem = L - floor(L / s) * s}.
\item If \code{rem} is at least the tolerance \code{seg_tol_frac * s}, it is large enough
to stand on its own and becomes an extra segment.
\item If \code{rem} is smaller than the tolerance, it is absorbed instead: one segment
is stretched to \code{s + rem}.
}

Either way the leftover is assigned to a \strong{randomly chosen} segment rather
than always the last one, so that the short (or long) segment is not
systematically at the end of every track. The target lengths always sum to
\code{L}.
}

\section{Reproducibility}{

The random choice makes segmentation non-deterministic. Pass a \code{seed} to fix
it. The RNG state of the calling session is left unchanged either way.
}

\examples{
tracks <- tibble::tibble(
  DATE = as.Date("2024-04-01"), new_trackno = "1",
  track_effort = 23, start_time = 120000
)
plan_segments(tracks, seg_length = 5, seed = 1)

}
\seealso{
\code{\link[=cut_segments]{cut_segments()}}, \code{\link[=segment_survey]{segment_survey()}}
}
