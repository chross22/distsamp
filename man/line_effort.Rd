% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/line-effort.R
\name{line_effort}
\alias{line_effort}
\title{Effort per survey line}
\usage{
line_effort(x, combine = c("occupation", "line"))
}
\arguments{
\item{x}{Point-level survey data with \code{pt2pt.effort}, \code{LEGNO}, and ideally
\code{LEGNO3}, or a \code{distsamp_segments} object.}

\item{combine}{\code{"occupation"} (default) or \code{"line"}.}
}
\value{
A tibble. Per occupation: \code{DATE}, \code{FILEID}, \code{LEGNO}, \code{LEGNO3},
\code{occupation}, \code{effort_km}, \code{n_records}. Per line: \code{LEGNO}, \code{effort_km},
\code{n_occupations}, \code{n_days}.
}
\description{
Summarises on-effort distance by survey line rather than by continuous track.
}
\section{Why this is not \code{segs$tracks}}{

A \strong{track} is a continuous run of effort: it ends wherever effort breaks,
which may be mid-line, and it says nothing about which line was being flown.
A \strong{line} is a design element — the transect the survey set out to fly, named
by \code{LEGNO}. Segmentation works on tracks, so \code{segs$tracks} cannot answer "how
much of line 7 did we actually cover, and did we have to go back for it".
}

\section{Occupations and lines}{

A line can be started, abandoned for weather, and flown again hours later.
Each attempt is an \strong{occupation}, identified by \code{LEGNO3} from
\code{\link[=make_leg_id]{make_leg_id()}}; the \strong{line} is \code{LEGNO} itself.

\code{combine = "occupation"} (the default) gives one row per attempt.
\code{combine = "line"} gives one row per line with its attempts summed, which is
the total coverage that line received.
}

\section{What it sums}{

\code{pt2pt.effort}, which \code{\link[=point_to_point_effort]{point_to_point_effort()}} attributes to the first
record of each on-effort pair and sets to zero across breaks. Summing it over
an occupation therefore gives distance actually flown on effort, not the
distance between the line's endpoints.
}

\section{Give it point-level data, not a segmentation}{

Passing a \code{distsamp_segments} uses its \code{points} table, which holds only the
records that reached a segment.

Usually that costs nothing: the records segmentation leaves out are off
effort, and an off-effort record carries zero \code{pt2pt.effort} — so dropping it
removes no distance. But a track shorter than \code{min_track_km}, or a segment
shorter than \code{min_segment_km}, is discarded \emph{with} its on-effort distance, and
that effort was really flown. A line made up of such a track then reports less
effort than the survey gave it, or disappears from the summary entirely.

So pass the point-level data after \code{\link[=flag_effort]{flag_effort()}} and
\code{\link[=point_to_point_effort]{point_to_point_effort()}} when the total has to be right. A message says which
you gave it.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
dat <- point_to_point_effort(flag_effort(make_leg_id(read_narwc(path))))

# One row per attempt at a line
line_effort(dat)

# One row per line, attempts combined
line_effort(dat, combine = "line")

}
\references{
Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}, section 8.A.19 (\code{LEGNO}). NARWC
Reference Document 2023-01.
}
\seealso{
\code{\link[=reflight_summary]{reflight_summary()}}, \code{\link[=track_effort]{track_effort()}}, \code{\link[=make_leg_id]{make_leg_id()}}
}
