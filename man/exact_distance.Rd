% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exact.R
\name{exact_distance}
\alias{exact_distance}
\title{Perpendicular distances from exact sighting positions}
\usage{
exact_distance(
  dat,
  by = NULL,
  units = c("m", "km", "nmi"),
  on_effort_only = TRUE
)
}
\arguments{
\item{dat}{A data frame of NARWC survey data with \code{LATITUDE}, \code{LONGITUDE},
and ideally \code{S_LAT}/\code{S_LONG}. Data without the sighting-position columns
comes back with the columns present and all \code{NA}.}

\item{by}{Grouping columns identifying one occupation of a survey line, passed
to \code{\link[=track_bearing]{track_bearing()}}.}

\item{units}{\code{"m"} (default), \code{"km"}, or \code{"nmi"}.}

\item{on_effort_only}{Restrict to on-effort census records. Default \code{TRUE}.}
}
\value{
A tibble with one row per row of \code{dat}:
\describe{
\item{\code{distance}}{Perpendicular distance from the track-line.}
\item{\code{along}}{Signed along-track offset between the logged position and
the point where the sighting was abeam.}
\item{\code{side}}{\code{"left"}, \code{"right"}, \code{"on-track"}, or \code{NA}.}
\item{\code{bearing}}{The track bearing used, for diagnosis.}
}
}
\description{
Computes the perpendicular distance from the track-line to each sighting that
carries an exact position in \code{S_LAT}/\code{S_LONG} (handbook 8.A.33, 8.A.34).
}
\section{Where this fits}{

This is the most direct of the three right-angle distance sources this package
supports, because it measures the quantity a detection function is defined on
rather than inferring it. It needs the sighting position to have been fixed
independently — from a GPS mark, a photograph, or a circling pass — so it is
available only where the survey recorded one, which in the NARWC archive is
mostly the NLPSC-era and later data. Where \code{S_LAT}/\code{S_LONG} and a declination
angle both exist, comparing the two is a useful check on both; they are
independent measurements of the same thing.
}

\section{How it differs from the original scripts}{

The processing scripts this package was rewritten from computed the
great-circle distance from the \emph{event position} straight to the sighting
position. That is a radial distance, not a perpendicular one, and it exceeds
the perpendicular distance by however far the aircraft was from being abeam
when the sighting was logged. This function projects onto the track instead,
and returns the along-track offset in \code{along} so the size of that difference
is visible rather than silently folded into the distance.
}

\section{Restricted to census records}{

Like \code{\link[=sighting_distances]{sighting_distances()}}, and for the same reason, distances are computed
only for on-effort records on a census line at \code{LEGSTAGE == 2} by default.
Off the census line there is no track-line to be perpendicular to: a position
logged while circling has a well-defined distance to the animal and no useful
perpendicular distance at all. Setting \code{on_effort_only = FALSE} computes them
anyway, which is occasionally useful for diagnostics and never appropriate for
fitting.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
dat <- flag_effort(make_leg_id(read_narwc(path)))
d <- exact_distance(dat)
cbind(dat[!is.na(d$distance), c("SPECCODE", "S_LAT", "S_LONG")],
      d[!is.na(d$distance), c("distance", "along", "side")])

}
\references{
Kenney, R.D. (2023) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 8}, sections 8.A.33 and 8.A.34.
NARWC Reference Document 2023-01.
}
\seealso{
\code{\link[=cross_track_distance]{cross_track_distance()}}, \code{\link[=track_bearing]{track_bearing()}}, \code{\link[=sighting_distances]{sighting_distances()}},
\code{\link[=strip_distance]{strip_distance()}}
}
