% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strip.R
\name{strip_distance}
\alias{strip_distance}
\title{Convert STRIP codes to distance intervals}
\usage{
strip_distance(
  strip,
  platform = c("skymaster", "at-11"),
  scheme = c("auto", "cetap", "nlpsc"),
  date = NULL,
  units = c("m", "km", "nmi"),
  left_truncation = FALSE
)
}
\arguments{
\item{strip}{Integer vector of \code{STRIP} codes.}

\item{platform}{\code{"skymaster"} (default) or \code{"at-11"}.}

\item{scheme}{\code{"auto"}, \code{"cetap"}, or \code{"nlpsc"}.}

\item{date}{Vector of survey dates, used when \code{scheme = "auto"}.}

\item{units}{\code{"m"} (default), \code{"km"}, or \code{"nmi"}.}

\item{left_truncation}{Apply the Skymaster blind-spot offset? Default \code{FALSE}.}
}
\value{
A tibble with one row per input: \code{distbegin}, \code{distend}, \code{side},
\code{scheme}.
}
\description{
Resolves each \code{STRIP} code to the right-angle distance interval it stands
for, and the side of the track it was on.
}
\section{Choosing the scheme}{

With \code{scheme = "auto"} (the default) the scheme is chosen from \code{date}: the
NLPSC / Mass CEC breakpoints for October 2011 onwards, the CETAP ones before.
That is a reasonable default but not a guarantee — a survey flown after 2011
under the older protocol would be misread — so pass \code{scheme} explicitly when
you know which applies. With no \code{date} and no \code{scheme}, this errors rather
than guess.
}

\section{The Skymaster blind spot}{

Handbook 8.A.31 notes that the Skymaster's restricted downward visibility
means CETAP-era distances from that aircraft "are actually measured from
about 1/8 mile to either side of the survey line". The strip boundaries do
not record this, so distances near zero are not what they appear: the region
under the aircraft was never searched. Set \code{left_truncation = TRUE} to shift
the innermost bins accordingly, and see \code{detection_data()} (not yet implemented) for fitting with a
left truncation, which is the statistically correct treatment.
}

\examples{
strip_distance(c(5, 6, 13), scheme = "nlpsc")

# Same codes, different era, different distances
strip_distance(c(5, 6, 13), scheme = "cetap")

}
\seealso{
\code{\link[=narwc_strip_bins]{narwc_strip_bins()}}, \code{\link[=sighting_distances]{sighting_distances()}}
}
