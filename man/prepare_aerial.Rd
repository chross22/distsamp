% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare_aerial}
\alias{prepare_aerial}
\title{Get a NARWC extract ready to segment}
\usage{
prepare_aerial(
  dat,
  platform = c("aerial", "all"),
  fill_legstage = TRUE,
  correct = NULL,
  effort_args = list(),
  quiet = FALSE
)
}
\arguments{
\item{dat}{A NARWC data frame from \code{narwcr::read_narwc()}.}

\item{platform}{Which platform to keep: \code{"aerial"} (default), or \code{"all"} to
classify without filtering. This package is aerial by construction — its
effort criteria and \code{perp_distance()} both assume an aircraft.}

\item{fill_legstage}{Reconstruct the line state where no \code{LEGSTAGE} was
written? Default \code{TRUE}. See \code{narwcr::fill_legstage()}.}

\item{correct}{A function applied to the filtered frame \emph{before} effort is
flagged, or \code{NULL}. This is where a correction that only applies to the
aerial records goes — an altitude recorded in feet, most often. It has to
happen here: \code{flag_effort()} reads \code{ALT}, so correcting afterwards leaves
every record failing the altitude ceiling, and correcting before the
platform filter scales ship altitudes that were already metres.}

\item{effort_args}{Named list passed to \code{narwcr::flag_effort()}, for a
programme whose criteria differ from the CETAP defaults.}

\item{quiet}{Suppress the running commentary. Default \code{FALSE}.}
}
\value{
\code{dat} with \code{LEGNO2}, \code{LEGNO3}, \code{LEGSTAGE_FILLED}, \code{PLATFORM_KIND}
and \code{OnOff.Effort} added, filtered to \code{platform}.
}
\description{
Runs the steps between reading a file and segmenting it, in the order they
have to happen: line occupations, line state, platform, effort. Each is
exported and can be run by hand; this exists because the order is not
obvious and getting it wrong is quiet.
}
\section{The order, and why it is not obvious}{

\describe{
\item{\code{make_leg_id()} first}{Everything downstream groups by \code{LEGNO3}.}
\item{\code{fill_legstage()} second}{It needs \code{LEGNO3} to know where an
occupation ends, and it must run before effort, because a right-angle
distance needs \code{LEGSTAGE == 2} (handbook 8.A.31). On a real archive
1,928 of 2,280 on-effort census sightings were ineligible without it —
for a code recorded only when it changed.}
\item{\code{classify_platform()} third, and the filter \emph{after} \code{make_leg_id()}}{
Removing records before occupations are built makes two occupations of
one line adjacent, so they merge and the ferry between them becomes
survey effort. Measured at 224.5 km where 4.4 km was right.}
\item{\code{flag_effort()} last}{It reads \code{LEGTYPE}, \code{LEGSTAGE}, \code{ALT},
\code{BEAUFORT} and \code{VISIBLTY}, so anything that corrects those has to have
happened already.}
}
}

\section{What it deliberately does not do}{

Anything that is an assertion about a particular file rather than a fact
about NARWC data. Mapping a declination angle out of a column the handbook
does not name, and correcting an altitude recorded in feet, are claims only
you can make. The first goes before this call; the second goes in \code{correct},
because where it happens changes what it does:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{dat <- narwcr::angles_from_declination(dat, "Decl_Angle", "Left_or_Right")

air <- prepare_aerial(dat, correct = function(x) \{
  feet <- !is.na(x$ALT) & x$DATE >= as.Date("2024-01-01")
  x$ALT[feet] <- x$ALT[feet] * 0.3048
  x
\})
}\if{html}{\out{</div>}}

\code{correct} runs after the platform filter and before effort is flagged, which
is the only place it can go: applying it earlier scales ship altitudes that
were already metres, and applying it later leaves every record failing the
altitude ceiling that \code{flag_effort()} has already tested.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
dat <- narwcr::read_narwc(path, quiet = TRUE)
air <- prepare_aerial(dat, quiet = TRUE)
table(air$PLATFORM_KIND)

}
\seealso{
\code{\link[=diagnose_pipeline]{diagnose_pipeline()}} to check the result, \code{\link[=plot_survey]{plot_survey()}} to look
at it, \code{\link[=segment_survey]{segment_survey()}} for what comes next.
}
