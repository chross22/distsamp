% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geodist.R
\name{gc_bearing}
\alias{gc_bearing}
\title{Initial great-circle bearing between positions}
\usage{
gc_bearing(lat1, lon1, lat2, lon2)
}
\arguments{
\item{lat1, lon1}{Numeric vectors of latitudes and longitudes of the start
positions, in decimal degrees. West longitudes are negative.}

\item{lat2, lon2}{Numeric vectors of the positions bearing is taken to.
Recycled against \code{lat1}/\code{lon1}.}
}
\value{
A numeric vector of bearings in \verb{[0, 360)}. \code{NA} where any coordinate
is \code{NA}, and \code{NA} where the two positions coincide, which has no bearing.
}
\description{
The bearing, in degrees clockwise from true north, along which a great circle
leaves the first position heading for the second.
}
\section{Initial, not constant}{

A great circle changes bearing as it is followed, so this is the bearing \emph{at
the first position}. Over the distances between consecutive survey positions
the change is negligible — a tenth of a degree over 10 km at these latitudes —
but it is the reason \code{\link[=cross_track_distance]{cross_track_distance()}} takes a bearing and a point
together rather than a bearing alone.
}

\examples{
# Due north along a meridian
gc_bearing(43, -69, 44, -69)

# Due east, at the moment of departure
gc_bearing(43, -69, 43, -68)

# A position has no bearing to itself
gc_bearing(43, -69, 43, -69)

}
\seealso{
\code{\link[=gc_distance]{gc_distance()}}, \code{\link[=cross_track_distance]{cross_track_distance()}}, \code{\link[=track_bearing]{track_bearing()}}
}
