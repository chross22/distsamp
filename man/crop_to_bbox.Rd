% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{crop_to_bbox}
\alias{crop_to_bbox}
\title{Restrict data to a bounding box}
\usage{
crop_to_bbox(x, bbox, coords = c("LONGITUDE", "LATITUDE"))
}
\arguments{
\item{x}{A data frame with longitude and latitude columns, an \code{sf} object, or
a \code{distsamp_segments} object. For a \code{distsamp_segments} object both the
\code{segments} and \code{points} tables are cropped, the former on its midpoint.}

\item{bbox}{Either a numeric vector with names \code{xmin}, \code{xmax}, \code{ymin},
\code{ymax}, or an object \code{\link[sf:st_bbox]{sf::st_bbox()}} understands.}

\item{coords}{Names of the longitude and latitude columns, used for plain
data frames. Defaults to \code{c("LONGITUDE", "LATITUDE")}, falling back to the
midpoint columns for a segment table.}
}
\value{
The input, cropped, with the same class.
}
\description{
Keeps only the records or segments falling inside a rectangular area.
}
\details{
Replaces the \code{crop_data()} helper that the original scripts called but never
defined.
}
\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
dat <- read_narwc(path)
gom <- c(xmin = -71, xmax = -66, ymin = 42, ymax = 45)
nrow(crop_to_bbox(dat, gom))

}
