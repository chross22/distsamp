% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segments.R
\name{cut_segments}
\alias{cut_segments}
\title{Cut tracks into segments}
\usage{
cut_segments(plan, dat, min_segment_km = 1, seed = NULL)
}
\arguments{
\item{plan}{A tibble from \code{\link[=plan_segments]{plan_segments()}}.}

\item{dat}{The point-level survey data, with \code{DATE}, \code{new_trackno},
\code{pt2pt.effort}, and \code{EVENTNO}.}

\item{min_segment_km}{Segments shorter than this are dropped from the result.
Default \code{1}.}

\item{seed}{Integer RNG seed for the coin flips, or \code{NULL}.}
}
\value{
The point-level data, restricted to records that fell inside a kept
segment, with columns added:
\describe{
\item{\code{seg_no}}{Position of the segment within its track.}
\item{\code{seg_id}}{Unique segment identifier, \code{DATE_track_segno}.}
\item{\code{seg_eff}}{Realised segment length in km, repeated on each record.}
\item{\code{events}}{\code{EVENTNO} range spanned, as \code{"first_last"}.}
\item{\code{case}}{Which rule ended the segment; see Details.}
}
}
\description{
Walks each continuous track record by record, accumulating point-to-point
effort until the target length for the current segment is reached, then
starts the next segment. This is the cutting half of the segmentation;
\code{\link[=plan_segments]{plan_segments()}} supplies the targets.
}
\details{
The \code{case} column records why each segment ended, preserving the diagnostic
labels from the original implementation:
\describe{
\item{\code{case2}}{The first remaining record alone met the target.}
\item{\code{case3}}{Several records were needed; the coin flip decided whether
to include the record that crossed the target.}
\item{\code{case4}}{Not enough effort remained to reach the target, so the
segment took what was left.}
\item{\code{case5}}{Last segment of the track; it absorbed all remaining
records.}
}
}
\section{Where the cut falls}{

Segments are cut at record boundaries — a survey record is never split — so a
segment rarely lands exactly on its target. For each segment the function
finds the first record at which cumulative effort reaches the target, then
flips a coin: heads, that record is included and the segment runs slightly
long; tails, it is excluded and the segment runs slightly short. Over many
segments the two errors cancel instead of biasing every segment long.

The final segment of each track takes all remaining records, so no effort is
lost off the end of a track.
}

\seealso{
\code{\link[=plan_segments]{plan_segments()}}, \code{\link[=segment_survey]{segment_survey()}}
}
