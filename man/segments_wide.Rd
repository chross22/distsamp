% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment-survey.R
\name{segments_wide}
\alias{segments_wide}
\title{Segment table with one column per species}
\usage{
segments_wide(x, value = c("animals", "sightings"), prefix = "n_")
}
\arguments{
\item{x}{A \code{distsamp_segments} object.}

\item{value}{Which count to spread: \code{"animals"} (default, the sum of
\code{NUMBER}) or \code{"sightings"} (the number of sighting records).}

\item{prefix}{Prefix for the generated count columns. Default \code{"n_"}.}
}
\value{
A tibble: the \code{segments} table with one count column per species.
Segments with no sightings of a species get \code{0}, not \code{NA}.
}
\description{
Reshapes a \code{\link[=segment_survey]{segment_survey()}} result into the layout density surface models
expect: one row per segment, with a count column for each species.
}
\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
segs <- segment_survey(read_narwc(path), seg_length = 5, seed = 1)
segments_wide(segs)

}
\references{
Miller, D.L., Burt, M.L., Rexstad, E.A. and Thomas, L. (2013) Spatial models
for distance sampling data: recent developments and future directions.
\emph{Methods in Ecology and Evolution} 4:1001-1010.
\doi{10.1111/2041-210X.12105}
}
