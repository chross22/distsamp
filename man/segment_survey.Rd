% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment-survey.R
\name{segment_survey}
\alias{segment_survey}
\title{Segment a line-transect survey}
\usage{
segment_survey(
  dat,
  seg_length,
  species = NULL,
  seed = NULL,
  seg_tol_frac = 0.5,
  min_track_km = 1,
  min_segment_km = 1,
  dist_method = c("haversine", "becker", "kenney", "eab", "rdk"),
  circling = c("same_species", "all", "none"),
  effort_args = list(),
  sighting_args = list()
)
}
\arguments{
\item{dat}{NARWC survey data, ideally from \code{\link[=read_narwc]{read_narwc()}}.}

\item{seg_length}{Target segment length in km.}

\item{species}{Character vector of \code{SPECCODE} values to count, or \code{NULL} for
all.}

\item{seed}{Integer RNG seed, or \code{NULL} for unseeded (not reproducible).}

\item{seg_tol_frac}{Passed to \code{\link[=plan_segments]{plan_segments()}}.}

\item{min_track_km}{Passed to \code{\link[=track_effort]{track_effort()}}.}

\item{min_segment_km}{Passed to \code{\link[=cut_segments]{cut_segments()}}.}

\item{dist_method}{Great-circle distance method: \code{"haversine"} (default),
\code{"becker"}, or \code{"kenney"}. See \code{\link[=gc_distance]{gc_distance()}} and \code{\link[=dist_methods]{dist_methods()}}.
Becker and Kenney are the same formula and give identical results.}

\item{circling}{How to handle sightings recorded while circling off the
census track: \code{"same_species"} (default), \code{"all"}, or \code{"none"}. See
\code{\link[=attach_circling_sightings]{attach_circling_sightings()}}.}

\item{effort_args}{Named list of arguments for \code{\link[=flag_effort]{flag_effort()}}, used only
when \code{dat} has no \code{OnOff.Effort} column.}

\item{sighting_args}{Named list of arguments for \code{\link[=segment_sightings]{segment_sightings()}}.}
}
\value{
An object of class \code{distsamp_segments}: a list with
\describe{
\item{\code{segments}}{One row per segment — \code{seg_id}, \code{DATE}, \code{FILEID},
\code{LEGNO}, \code{LEGNO3}, \code{new_trackno}, \code{seg_no}, \code{seg_eff} (km),
\code{mid_lat}, \code{mid_lon}, \code{mean_beaufort}, \code{wt_beaufort}, \code{n_records},
\code{events}, \code{case}, \code{start_time}.}
\item{\code{sightings}}{One row per segment per species.}
\item{\code{tracks}}{One row per continuous track.}
\item{\code{points}}{The point-level data with segment assignments, for
diagnostics and mapping.}
\item{\code{call}, \code{settings}}{The call and the parameters used.}
}
}
\description{
Runs the whole segmentation pipeline: identify line occupations, flag effort,
accumulate along-track distance, split lines where effort breaks, plan how
many segments each continuous track carries, cut them, and summarise
sightings and conditions onto each segment.
}
\details{
This is the function most users want. The individual steps are exported too,
so you can run them yourself when you need to intervene between stages.
}
\section{Pipeline}{

\enumerate{
\item \code{\link[=make_leg_id]{make_leg_id()}} — separate re-occupations of the same survey line.
\item \code{\link[=flag_effort]{flag_effort()}} — decide which records are on effort.
\item \code{\link[=point_to_point_effort]{point_to_point_effort()}} — great-circle distance between consecutive
on-effort positions.
\item \code{\link[=split_tracks]{split_tracks()}} — start a new track wherever effort breaks.
\item \code{\link[=track_effort]{track_effort()}} — total effort per continuous track.
\item \code{\link[=plan_segments]{plan_segments()}} — how many segments, and how long each should be.
\item \code{\link[=cut_segments]{cut_segments()}} — walk the records and make the cuts.
\item \code{\link[=segment_midpoints]{segment_midpoints()}} — the along-track midpoint of each segment.
\item \code{\link[=segment_sightings]{segment_sightings()}} — counts and conditions per segment.
}

Steps 1 and 2 are skipped when the input already carries \code{LEGNO3} or
\code{OnOff.Effort} respectively, so you can substitute your own definitions.
}

\section{Reproducibility}{

Segmentation makes two random choices: which segment absorbs a track's
leftover distance, and whether a segment that cannot land exactly on its
target runs slightly long or slightly short. Pass a \code{seed} to make a run
repeatable. The calling session's RNG state is never disturbed.
}

\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
segs <- segment_survey(read_narwc(path), seg_length = 5, seed = 1)
segs
segs$segments[, c("seg_id", "seg_eff", "mid_lat", "mid_lon")]

}
\references{
Becker, E.A., Forney, K.A., Ferguson, M.C., Foley, D.G., Smith, R.C., Barlow,
J. and Redfern, J.V. (2010) Comparing California Current cetacean-habitat
models developed using in situ and remotely sensed sea surface temperature
data. \emph{Marine Ecology Progress Series} 413:163-183.
\doi{10.3354/meps08696}

Hedley, S.L. and Buckland, S.T. (2004) Spatial models for line transect
sampling. \emph{Journal of Agricultural, Biological, and Environmental Statistics}
9:181-199. \doi{10.1198/1085711043578}

Miller, D.L., Burt, M.L., Rexstad, E.A. and Thomas, L. (2013) Spatial models
for distance sampling data: recent developments and future directions.
\emph{Methods in Ecology and Evolution} 4:1001-1010.
\doi{10.1111/2041-210X.12105}

Kenney, R.D. (2021) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 7}. NARWC Reference Document
2021-01. University of Rhode Island, Graduate School of Oceanography.
}
\seealso{
\code{\link[=segments_wide]{segments_wide()}} for a segment table with one count column per
species, which is the shape \code{dsm} expects.
}
