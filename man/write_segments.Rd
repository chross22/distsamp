% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{write_segments}
\alias{write_segments}
\title{Write segmentation results to CSV}
\usage{
write_segments(
  x,
  dir,
  prefix = "segments",
  tables = c("segments", "sightings", "tracks", "points")
)
}
\arguments{
\item{x}{A \code{distsamp_segments} object.}

\item{dir}{Directory to write to. Created if it does not exist.}

\item{prefix}{Filename prefix. Default \code{"segments"}.}

\item{tables}{Which tables to write. Default all four.}
}
\value{
The paths written, invisibly.
}
\description{
Writes the tables from a \code{\link[=segment_survey]{segment_survey()}} result to a directory. Nothing
else in the package writes to disk, so output is always something you asked
for.
}
\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
segs <- segment_survey(read_narwc(path), seg_length = 5, seed = 1)
out <- file.path(tempdir(), "segout")
write_segments(segs, out)
list.files(out)

}
