% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circling.R
\name{flag_circling}
\alias{flag_circling}
\title{Flag records made while circling}
\usage{
flag_circling(dat)
}
\arguments{
\item{dat}{A data frame of NARWC survey data in survey order, with \code{LEGTYPE}
and ideally \code{LEGSTAGE} and \code{LEGNO3}.}
}
\value{
\code{dat} with an integer \code{CIRCLE} column: \code{1} while circling, \code{0}
otherwise.
}
\description{
Marks the records where the aircraft had left the census track to circle a
sighting.
}
\details{
Two signals are used, either of which is sufficient:
\itemize{
\item \code{LEGTYPE == 4}, the line-transect "other (circling)" code (handbook
8.A.20); and
\item any record between a \code{LEGSTAGE == 3} (break off line to circle) and the
following \code{LEGSTAGE == 4} (resume line) within the same survey line
occupation (handbook 8.A.19).
}

The second signal matters because a survey may log positions during circling
without changing \code{LEGTYPE}, and because the break-off and resume records
themselves stay at \code{LEGTYPE == 2}.
}
\examples{
path <- system.file("extdata", "narwc-example.csv", package = "distsamp")
dat <- flag_circling(make_leg_id(read_narwc(path)))
sum(dat$CIRCLE)

}
\references{
Kenney, R.D. (2021) \emph{The North Atlantic Right Whale Consortium Database: A
Guide for Users and Contributors, Version 7}, sections 8.A.19 and 8.A.20. NARWC Reference Document
2021-01.
}
